// import * as Types from '../constants/ActionTypes';

// var INITIAL = {
//     isConnected: true
// };

// const NetInfo = (state = INITIAL, action) => {
//     switch (action.type) {
//         case Types.CHANGE_CONNECTION_STATUS:
//             return { ...state, isConnected: action.status };
//         default:
//             return state;
//     }
// }

// export default NetInfo;